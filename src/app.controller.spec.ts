/*import { Test, TestingModule } from '@nestjs/testing';
import { AppController } from './app.controller';
// AppService is not exported from './app.service' — we'll mock the service in the test instead

describe('AppController', () => {
  let appController: AppController;

  beforeEach(async () => {
    // create a simple mock of the service used by AppController
    const mockService = { getHello: () => 'Hello World!' };
    appController = new AppController(mockService as any);
  });

  describe('root', () => {
    it('should return "Hello World!"', () => {
      expect(appController.getProducts()).toBeDefined();
    });
  });
});*/
